import { OpenNextConfig } from '@opennextjs/cloudflare';

const config: OpenNextConfig = {
  // Hanya jika Anda perlu mengkonfigurasi lebih lanjut
  // Jika tidak, biarkan objek konfigurasi kosong
};

export default config;
