// open-next.config.ts

/** @type {import('@opennextjs/cloudflare').OpenNextConfig} */
const config = {};

// Mengekspor sebuah objek yang memiliki kunci 'default'
// yang berisi objek konfigurasi OpenNext yang sebenarnya.
module.exports = {
  default: config,
};
